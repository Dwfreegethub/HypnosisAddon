{
  "name": "hypnosisaddon",
  "version": "0.20.2",
  "description": "BC Hypnosis Add-on — trust-based hypnosis mechanics for Bondage Club, shipped as a Tampermonkey userscript.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node build-test.mjs && node test/run.mjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@types/lz-string": "^1.3.34",
    "bondage-club-mod-sdk": "^1.2.0",
    "lz-string": "^1.5.0"
  }
}
