{
  "name": "hypnosisaddon",
  "version": "0.85.3",
  "description": "Erotic Chat Hypnosis Suite (ECHS) — trust-based hypnosis mechanics for Bondage Club, shipped as a Tampermonkey userscript.",
  "scripts": {
    "build": "node build.mjs",
    "release": "node release.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node build-test.mjs && node test/run.mjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@types/lz-string": "^1.3.34",
    "bondage-club-mod-sdk": "^1.2.0",
    "lz-string": "^1.5.0"
  }
}
