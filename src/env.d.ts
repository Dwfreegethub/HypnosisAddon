// Injected by build.mjs via esbuild's `define`, from package.json's version — single
// source of truth so the on-screen indicator can never drift from what actually built.
declare const __VERSION__: string;
